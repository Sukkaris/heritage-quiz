# 世界遺産クイズ 開発経過

最終更新: 2026-07-30

公開URL: https://sukkaris.github.io/heritage-quiz/
リポジトリ: https://github.com/Sukkaris/heritage-quiz （公開）
ローカル: `D:\Claude Code\heritage-quiz\`

---

## 1. いまどうなっているか

企画書の第一版が完成し、GitHub Pagesで公開・稼働中。iPhoneのSafariで開いて
「ホーム画面に追加」すればアプリとして起動し、**オフラインでも動く**。

- 遺産マスタ **1,273件**（UNESCO公式と一致。うち出題可能 **1,270件**）
- 出題形式 **4種類**（所在国 / 区分 / 遺産名 / 登録基準）
- 弱点モード、4種類のフィルタ、学習記録、JSON書き出し・読み込み

---

## 2. 進め方（企画書§10.3の段階）

| 段階 | 内容 | 状態 |
|---|---|---|
| ① | データ取得スクリプト | 完了 |
| ② | 検品レポート | 完了（[DATA_REPORT.md](DATA_REPORT.md)） |
| ③ | 最小UIで1テンプレート動作確認 | 完了 |
| ④ | 残りのテンプレート | 完了（第二版の4・5も実装済み） |
| ⑤ | 学習機能 | 完了 |
| ＋ | iPhone対応・公開 | 完了 |

---

## 3. データ取得で分かったこと

### 3.1 Wikidataのプロパティ（推測せず実物を見て確定した）

| 項目 | 実際の在りか |
|---|---|
| 世界遺産の絞り込み | `P1435`（遺産保護指定）＝ `Q9259`（世界遺産） |
| 登録年 | `P1435` ステートメントの**修飾子 `P580`**（プロパティ直下ではない） |
| 登録基準 | `P2614`（値は (i)〜(x) の10アイテム） |
| UNESCO ID | `P757` |
| 所在国 | `P17` |
| 区分（文化/自然/複合） | **直接持つプロパティは存在しない** |

区分は企画書§3.3の手順どおり、登録基準から導出した（i〜vi=文化、vii〜x=自然、両方=複合）。

### 3.2 件数の突き合わせ（2026-08-02に決着）

当初は1,274件で「公式1,248件＋2026年登録25件でほぼ説明がつく」としていたが、
**1件多い**理由が未解明だった。UNESCO公式の一覧（公開XML）を取得して照合したところ、
正しい件数は **1,273件** と確定し、原因は4つに分解できた。

| 原因 | 内容 | 対処 |
|---|---|---|
| 登録抹消・統合の混入 | Wikidataは抹消や統合を `P1435` の修飾子 **`P582`（終了日）** で表す。アラビアオリックスの保護区（2007年抹消）、バージェス頁岩（1984年に統合）、シャンボール城（2000年に統合）が混ざっていた | 取得クエリで `P582` を持つ指定を除外 |
| 構成資産による乗っ取り | `P757` には構成資産のID（`868-062` = 巡礼路の62番目 = アミアン大聖堂）も入る。これを親遺産のIDとして扱っていたため、サイトリンク数の多い構成資産が親遺産の名前を乗っ取っていた（ID669が「ブルゴス大聖堂」になっていた） | 数字＋`rev`/`bis`/`ter`/`quater` のみを遺産IDとみなす |
| 取得漏れ | Query Serviceの障害中に取得したため、ビスカヤ橋など一部が結果に含まれていなかった | 障害復旧後に取り直し |
| ID欠落 | グアナフアト・フエなど、Wikidata側にUNESCO IDが入っていない遺産がある | 公式の英語名／日本語版Wikipediaの登録名から引いたQIDで対応づけ |

結果、**1,273件でUNESCO公式と完全一致**（区分・登録年・危機遺産の件数もすべて一致）。

なお、パナマ・ビエホ（ID790）は2026年に「パナマの植民地時代の地峡横断道路」(ID1582)へ
再編されており、公式一覧から消えている。Wikidataには残っているが本アプリでは除外した。

### 3.3 日本語名の欠落（企画書§3.4）

Wikidataの日本語ラベルだけでは **320件**が名無しになった。
日本語版Wikipediaの「世界遺産の一覧（地域別）」の表に**UNESCO IDと日本語の登録名が両方**
載っていることを見つけ、IDで突き合わせて補完。→ 欠落 **2件** まで減少。

出題可能件数は 943件 → **1,270件** に増えた。

### 3.4 登録基準の条文

テンプレート5で選択肢に条文を出すため、(i)〜(x) の日本語条文が必要だった。
Wikidataには (i)(ii) の条文が入っていなかったので、日本語版Wikipedia「世界遺産」の
テンプレートを展開して10件そろえた（世界遺産センター公式基準の翻訳・引用）。

### 3.5 取得時のトラブル

- **Wikidata Query Serviceが障害中**で、1リクエスト/分に制限されていた
  → 大きなクエリ1本ではなく軽量クエリ6本に分割し、自動で待って再試行する作りにした
- このPCはセキュリティソフトのHTTPS検査があるため、Pythonの標準の証明書検証が通らない
  → `truststore` でWindowsの証明書ストアを使うようにした

---

## 4. 実装した出題

すべてマスタから実行時に自動生成する（問題文のファイルは持たない）。

| 種類 | 設問 | 備考 |
|---|---|---|
| 所在国 | 「〇〇」があるのはどの国ですか？ | 複数国にまたがる遺産は除外 |
| 区分 | 「〇〇」は文化遺産・自然遺産・複合遺産のどれですか？ | 3択 |
| 遺産名 | 次のうち、〇〇にある世界遺産はどれですか？ | 誤答はその国に無い遺産のみ |
| 登録基準 | 「〇〇」の登録基準に含まれるものはどれですか？ | 誤答はその遺産が持たない基準のみ |

**登録年代を問う設問は、指示により削除した。** 登録年代はフィルタとしてのみ使う。

### 誤答の引き方が難易度のレバー（企画書§6.4）

難しい問題ほど、正解と紛らわしいところから誤答を引く。

| 難易度 | 4択中、紛らわしい誤答の数 |
|---|---|
| 易 | 0個（無関係な地域・逆系統から引く） |
| 中 | 1個 |
| 難 | 3個（同一地域の国・同一地域の遺産・同系統の基準から引く） |

登録基準の設問は、**その遺産が持っていない基準だけ**を誤答に使うので、
複数の基準を持つ遺産でも正解は必ず1つになる（企画書が懸念していた点）。

---

## 5. 検証したこと

| 内容 | 結果 |
|---|---|
| 全1,270件×5回＝**6,350問**を生成して検査 | 選択肢の重複 0件 / 正解の欠落 0件 / 選択肢数の誤り 0件 |
| 複数正解の検出（全テンプレートを全件） | **0件**（下記のバグ修正後） |
| UNESCO公式一覧との照合 | 件数・区分・登録年・危機遺産すべて一致（1,273件） |
| 難易度レバーの実測 | 所在国 0/1/3、遺産名 0/1/3、登録基準 0.03/0.99/2.66 |
| 10問通しの動作 | 正誤表示・補足・結果画面・学習記録すべて正常 |
| フィルタ | 地域・区分・年代・難易度の組み合わせが正しく効く |
| 弱点モード | 苦手な遺産が均等抽選の約6倍の頻度で出る |
| 手書き問題の受け入れ口 | 1問注入して混ざることを確認 |
| レイアウト（375px幅） | 横スクロールなし、選択肢56px、最長61文字の遺産名でもはみ出しなし |
| オフライン動作 | サーバーを停止した状態で再読み込みし、全件読み込めた |
| 本番URL | Service Worker稼働、10ファイルすべてキャッシュ、出題まで正常 |

※ 登録基準の難易度レバーが「難」で3.00にならないのは、遺産ごとに選べる基準の
母数が違うため（自分が持つ基準は誤答に使えない）。仕様どおりの挙動。

### 検証で見つかって直したバグ

**同名の遺産による複数正解（2026-08-02）**。「イグアス国立公園」はアルゼンチン(ID303)と
ブラジル(ID355)の2件が同名で存在する。選択肢には名前しか出ないため、
「次のうち、ブラジルにある世界遺産はどれですか？」の誤答にアルゼンチン側を使うと、
表示上は正解と区別がつかない状態になっていた。誤答を選ぶ際に、**同じ名前の遺産が
問われている国に存在しないこと**を条件に加えて修正した。

---

## 6. 判断したこと

企画書で「実装時に判断して報告すること」とされていた点。

- **複数国にまたがる遺産（61件）**: 複数の地域に属するものとして扱う。
  所在国を問う設問からは除外（複数正解になるため）
- **大陸をまたぐ国**: ロシア→ヨーロッパ、トルコ→アジア、カザフスタン→アジア、
  エジプト→アフリカ など。全件を [DATA_REPORT.md](DATA_REPORT.md) に一覧化
- **難易度の閾値**: サイトリンク数の分位点で 易=47以上 / 中=9〜46 / 難=8以下。
  `data.js` 冒頭の `DIFFICULTY` 1か所で調整できる
- **登録年の異常値**: セレンゲティ国立公園に1960年（国立公園の設置年）が混ざっていたため、
  1978〜2026年の範囲外は登録年として採用しない

---

## 7. 積み残し・注意点

- **日本語名が無い2件**は出題対象外（データとしては保持）。ヴィスビュー(731)と
  ドナウ・リーメス西部(1608)で、日本語版Wikipediaの一覧とIDが食い違っているもの
- **学習記録のバックアップ**: iPhoneのブラウザ内にしか無い。Safariのデータを消すと失われる。
  スタート画面の「学習記録の管理」からJSONで書き出せる。
  忘れないように、アプリ側から2通りで促している（2026-08-02追加）
  - スタート画面に「前回の書き出しから◯日たちました」を表示。14日を超えると色が変わる
  - 書き出さないまま100問を超えると、結果画面に一言と書き出しボタンが出る
  - しきい値は `app.js` の `BACKUP` 定数1か所（`warnAfterDays` / `remindAfterAnswers`）
- **更新時は `sw.js` の `CACHE_VERSION` を上げてからpushする**。上げないとiPhoneが
  古いキャッシュを使い続ける
- `file://` で直接開いた場合の動作は、実ブラウザでの確認が未了
  （`fetch` を使っていないので設計上は問題ないはず。PCではhttp経由で動作確認済み）

---

## 8. 年次更新の手順

世界遺産は毎年6〜7月の世界遺産委員会で新規登録がある。更新はデータの取り直しだけで、
アプリのコードは触らない。**1年後にスクリプトの中身を忘れていても、この節だけ読めば終わる**
ように書いてある。

### いつやるか

委員会の直後は、UNESCO公式一覧には載っていてもWikidata側が追いついていないことがある
（新規登録の遺産に日本語ラベルやサイトリンクが揃うまで数週間〜数か月かかる）。
**委員会から1〜2か月あけてから**実行するのが無難。Wikidata Query Serviceは障害中だと
1リクエスト/分に制限されることがあり、その間に取ると**結果が歯抜けになる**
（2026年に実際に起きた。§3.2参照）。取得後の件数チェックで気付けるようにしてある。

### 手順

**1. 古いデータを消す**（消したものだけ取り直す作りになっている）

条文（`criteria_ja.json`）と基準ラベル（`criteria_labels.json`）は変わらないので残す。

```bash
cd "D:/Claude Code/heritage-quiz/raw" && rm -f items.json countries.json criteria.json country_meta.json coords.json in_danger.json jawiki.json unesco_list.json jawiki_list.json jawiki_qid.json jawiki_category.json
```

**2. 取り直す**（この順番で実行する。`fetch_ja_qids.py` は `jawiki_list.json` を使うので順序が必要）

```bash
cd "D:/Claude Code/heritage-quiz" && uv run --with truststore python tools/fetch_unesco_list.py && uv run --with truststore python tools/fetch_jawiki_list.py && uv run --with truststore python tools/fetch_ja_qids.py && uv run --with truststore python tools/fetch_wikidata.py
```

`fetch_wikidata.py` はレート制限に合わせて待つので**10分ほどかかる**。
`UV_NATIVE_TLS is deprecated` という警告が出るが無視してよい。

**3. マスタを作る**

```bash
cd "D:/Claude Code/heritage-quiz" && uv run python tools/build_data.py && uv run --with truststore python tools/verify_category.py
```

**4. 確認する**（下の「確認項目」を見る）

**5. キャッシュ版を上げてpushする**

`sw.js` の `CACHE_VERSION` を `v3` → `v4` のように上げる。**これを忘れるとiPhoneが
古いままになる。**

```bash
cd "D:/Claude Code/heritage-quiz" && git add -A && git commit -m "2027年の登録分を反映" && git push
```

### スクリプトの入出力

| スクリプト | 読むもの | 作るもの |
|---|---|---|
| `fetch_unesco_list.py` | UNESCO公式XML | `raw/unesco_list.json`（IDの正解一覧） |
| `fetch_jawiki_list.py` | 日本語版Wikipediaの一覧記事 | `raw/jawiki_list.json`（ID→日本語の登録名） |
| `fetch_ja_qids.py` | `raw/jawiki_list.json` | `raw/jawiki_qid.json`（ID→QID） |
| `fetch_wikidata.py` | Wikidata | `raw/items.json` ほか7ファイル |
| `fetch_criteria_ja.py` | 日本語版Wikipedia | `raw/criteria_ja.json`（基準の条文。**再実行不要**） |
| `build_data.py` | `raw/` 一式 | `data.js` と `DATA_REPORT.md` |
| `verify_category.py` | `data.js` と `raw/unesco_list.json` | `CATEGORY_CHECK.md` |

日本語名の補完（§3.3）は `fetch_jawiki_list.py` が担っているので、**毎年やり直す**。
新規登録の遺産はWikidataに日本語ラベルが無いことが多く、これを飛ばすと新しい遺産が
出題対象から丸ごと抜ける。

### 確認項目

`DATA_REPORT.md` の「UNESCO公式一覧との突き合わせ」を見る。ここが全部そろっていれば正常。

| 見る場所 | 正常な状態 |
|---|---|
| 総件数 | 本アプリとUNESCO公式が**同じ数** |
| 文化 / 自然 / 複合 | 3つとも公式と一致 |
| 危機遺産 | 公式と一致 |
| 公式に無いのに本アプリにある | 0件 |
| 公式にあるのに本アプリに無い | 0件 |
| 区分・登録年が公式と食い違う | どちらも0件 |
| 出題可能件数 | 総件数からの目減りが数件以内 |
| 日本語ラベル欠落 | 数件以内（2026年時点は2件） |
| 登録年の欠損 | 0件 |
| `CATEGORY_CHECK.md` の不一致 | 0件 |

食い違いが出たら、原因の見当は §3.2 の表（抹消・統合、構成資産、取得漏れ、ID欠落）が
だいたい網羅している。件数が大きく足りない場合はQuery Serviceの取得漏れを疑い、
`raw/items.json` を消して取り直す。

年の上限は実行時の年を自動で使うので、書き換えは不要。難易度の閾値も再計算される。

---

## 9. これから足せるもの

- **手書き問題**（企画書§9）: `manual_questions.js` に書き足すだけで出題に混ざる。
  1セット10問のうち最大3問まで。「なぜこの登録基準なのか」など、テンプレートでは
  作れない設問はここに入れる
- **地図機能**: 座標を1,255件分すでに持っている（`data.js` の `coord`）
- **別ジャンルへの差し替え**: `data.js` を入れ替えれば、美術・絵画のクイズにできる構造

---

## 10. ファイルの役割

| ファイル | 中身 |
|---|---|
| `index.html` | 画面の骨組み |
| `style.css` | 見た目 |
| `data.js` | **自動生成**の遺産マスタ。難易度の閾値もここ |
| `app.js` | 出題ロジック・学習記録。遺産の事実は一切書いていない |
| `manual_questions.js` | 手書き問題の置き場（初期は空） |
| `sw.js` | オフライン用キャッシュ。更新時は `CACHE_VERSION` を上げる |
| `manifest.webmanifest` | ホーム画面に追加したときの見え方 |
| `DATA_REPORT.md` | データ検品レポート（UNESCO公式との照合結果を含む） |
| `CATEGORY_CHECK.md` | 区分（文化/自然/複合）導出の検算レポート |
| `PROGRESS.md` | この文書 |
| `tools/` | データ取得・生成スクリプト（アプリ実行時には使わない） |
| `raw/` | Wikidata・Wikipediaから取得した生データ |
